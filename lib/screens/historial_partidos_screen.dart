import 'package:flutter/material.dart';

class HistorialPartidosScreen extends StatelessWidget {
  const HistorialPartidosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Historial de partidos'),
      ),
      body: const Center(
        child: Text('Texto placeholder explicando qué irá aquí.'),
      ),
    );
  }
}
